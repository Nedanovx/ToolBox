@model CommentModel
@{
    ViewData["Title"] = "Delete comment";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3 class="text-danger">Потвърждение за изтриване</h3>
                </div>
                <div class="card-body">
                    <p><strong>Сигурни ли сте, че искате да изтриете този коментар?</strong></p>
                    <p><strong>Коментар:</strong> @Model.ProductComment</p>

                    <div class="d-flex justify-content-between">
                        <a asp-controller="Details" asp-action="RedirectToSku" asp-route-id="@Model.ProductId" class="btn btn-secondary mt-3">Отказ</a>
                        <form asp-controller="Comment" asp-action="DeleteConfirm" asp-route-id="@Model.Id" method="post">
                            <button type="submit" class="btn btn-danger">Изтрий коментара</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>